import CreatureProfile from './CreatureProfile'
import { vaquitaData } from './creatureProfileData'

const Vaquita = () => {
  return <CreatureProfile creature={vaquitaData} />
}

export default Vaquita